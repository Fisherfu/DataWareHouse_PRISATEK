# 開發記錄

專案：13 倉庫進出貨整合 Data Warehouse 提案 — 客戶 Demo 網站
Repo：https://github.com/Fisherfu/DataWareHouse_PRISATEK
線上網址：https://fisherfu.github.io/DataWareHouse_PRISATEK/

---

## 2026-08-20

### 背景
對應 `datawarehouse/Proposal_Integrated_13倉庫OCR_RPA_DataWarehouse提案.md` 提案中的第一項交付物（13 倉庫進出貨整合介面），客戶希望先看到 UI Demo 再進行後續開發。使用者提供一張現有介面截圖（庫存整合系統／整合看板）作為畫面參考。

### 完成項目
1. **建立 Demo UI**：依參考截圖打造「庫存整合系統」單頁 Demo，包含左側導覽、日期／搜尋／匯出Excel 頂欄、13 倉今日收件狀態卡（含逾時警示）、以及對應提案九大欄位（入庫時間／客戶／料號／客戶料號／數量／庫存天數／DC／LOT CODE／倉庫）的明細表。
   - 視覺系統另行設計：主色靛藍 `#2955D9`、字體搭配 Sora（標題/UI）＋ Noto Sans TC（中文內文）＋ IBM Plex Mono（數字／料號等等寬資料），支援亮／暗色模式。
   - 內含可互動假資料：搜尋框即時篩選、倉庫卡片可篩選、匯出按鈕顯示示範提示。
2. **佈署到 GitHub Pages**：
   - 使用者建立空 repo `Fisherfu/DataWareHouse_PRISATEK`，將 Demo 頁面以 `index.html` 推上 `main` 分支。
   - 於 Settings → Pages 設定 Source = Deploy from a branch / main / root，網站上線於 `https://fisherfu.github.io/DataWareHouse_PRISATEK/`。
   - 討論過 Custom Domain：需自有網域才能設定；若想要 `prisatek.github.io` 需另建 GitHub Organization `prisatek` + `prisatek.github.io` repo（免費，帳號名稱驗證為可申請）。**目前決定維持 `fisherfu.github.io` 網址，未設定 Custom Domain。**
3. **資訊架構調整（依使用者回饋反覆迭代）**：
   - 第一版：新增獨立「總表」頁面，依料號彙總跨 13 倉庫存量，作為進入系統的入口頁，與原本「整合看板」（單倉明細＋收件狀態監控）並列兩個分頁。
   - 第二版（最終版）：使用者認為兩頁可以合併，改為單一「**倉庫總覽**」頁面 —— 13 倉收件狀態卡＋逾時警示置頂，下方表格預設顯示「依料號彙總」總表；點選任一倉庫卡片即切換為該倉「單倉明細」表。
   - 加入「全部」卡片（虛線框、預設選中）讓使用者能明確點回彙總檢視，取代原本需要「再點一次同倉卡片取消篩選」的隱性操作。
   - 其餘導覽項目（庫存整合表單／主檔管理／通知中心／帳號與權限）暫為「規劃中」預告畫面，用於跟客戶說明第一階段範疇。
4. **版本記錄**：
   - Commit `230036e`：初版 Demo（整合看板）
   - Commit `0a52ed3`：合併總表與整合看板為「倉庫總覽」單一頁面

### 待確認 / 下一步
- 客戶看過 Demo 後的畫面回饋（是否符合預期、欄位是否需增減）。
- 若確定要做真實 SKU／客戶／倉庫代號命名，需要替換目前 Demo 中的假資料。
- Custom Domain 是否要正式申請（需使用者決定是否要買網域，或建立 `prisatek` GitHub Organization）。
- 「庫存整合表單」「主檔管理」「通知中心」「帳號與權限」等頁面何時排入開發，需對照提案 Page 6-7 的兩階段時程規劃。
